# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: open-chat-playground

services:  
  app:
    language: dotnet
    project: ./src/OpenChat.AppHost/OpenChat.AppHost.csproj
    host: containerapp
